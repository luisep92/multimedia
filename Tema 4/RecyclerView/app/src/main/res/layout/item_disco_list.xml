<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/relative_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/white"
    android:orientation="vertical">
    <ImageView
        android:id="@+id/iv_discoImage"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:padding="8dp"
        android:contentDescription="@string/desc_image"
        app:srcCompat="@mipmap/ic_launcher_round" />
    <TextView
        android:id="@+id/tv_tituloDisco"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginStart="5dp"
        android:layout_marginTop="10dp"
        android:layout_toEndOf="@+id/iv_discoImage"
        android:text="@string/titulo_text"
        android:textSize="30sp" />
    <TextView
        android:id="@+id/tv_autorDisco"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tv_tituloDisco"
        android:layout_alignBottom="@+id/iv_discoImage"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="5dp"
        android:layout_marginTop="10dp"
        android:layout_toEndOf="@+id/iv_discoImage"
        android:text="@string/autor_text"
        android:textSize="18sp" />
</RelativeLayout>